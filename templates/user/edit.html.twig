{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block content %}
    <br><br>
    <h1>Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <br>
    <a class="btn" href="{{ path('app_user_show', {'id': user.id}) }}">Back</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
